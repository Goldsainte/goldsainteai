// Deprecated standalone helper retained for backwards compatibility.
// Delegate to the service-layer implementation that handles escrow + commissions.
export { createBookingFromProposal } from "@/services/bookingService";
