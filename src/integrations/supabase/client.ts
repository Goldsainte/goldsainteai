// This file is automatically generated. Do not edit it directly.
import { createClient } from "@supabase/supabase-js";
import type { Database } from "./types";

const FALLBACK_SUPABASE_URL = "https://placeholder.supabase.co";
const FALLBACK_SUPABASE_KEY = "public-anon-key";

const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || FALLBACK_SUPABASE_URL;
const SUPABASE_PUBLISHABLE_KEY =
  import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY || FALLBACK_SUPABASE_KEY;

const isPlaceholderConfig =
  !SUPABASE_URL ||
  !SUPABASE_PUBLISHABLE_KEY ||
  SUPABASE_URL.includes("your-project.supabase.co") ||
  SUPABASE_PUBLISHABLE_KEY.includes("your-supabase-anon-key") ||
  SUPABASE_URL === FALLBACK_SUPABASE_URL ||
  SUPABASE_PUBLISHABLE_KEY === FALLBACK_SUPABASE_KEY;

if (import.meta.env.PROD && isPlaceholderConfig) {
  throw new Error(
    "Supabase client is not configured. Set VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY for production builds."
  );
}

if (import.meta.env.DEV && isPlaceholderConfig) {
  console.warn(
    "Using placeholder Supabase configuration. Set VITE_SUPABASE_URL and VITE_SUPABASE_PUBLISHABLE_KEY for full functionality."
  );
}

// Import the supabase client like this:
// import { supabase } from "@/integrations/supabase/client";

export const supabase = createClient<Database>(SUPABASE_URL, SUPABASE_PUBLISHABLE_KEY, {
  auth: {
    persistSession: true,
    autoRefreshToken: true,
    detectSessionInUrl: true,
  },
});