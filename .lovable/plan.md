

## Rename "Get Custom Itinerary" → "Design My Trip"

Replace the CTA text across all 4 files where it appears. Also update the supporting microcopy from "Designed for you · Delivered in 24–48 hours" to "Your dream trip, designed by a local expert" for consistency with the new wording.

| File | Line | Change |
|------|------|--------|
| `src/components/profile/ProfileHero.tsx` | 193 | `Get Custom Itinerary` → `Design My Trip` |
| `src/components/profile/ProfileSidebar.tsx` | 65 | `Get Custom Itinerary` → `Design My Trip` |
| `src/components/creator/CreatorStoryboardGrid.tsx` | 86 | `Get Custom Itinerary` → `Design My Trip` |
| `src/pages/creators/CreatorPublicProfilePage.tsx` | 470 | `Get Custom Itinerary` → `Design My Trip` |

Also update the "Plan a trip like this" hover CTA in `CreatorStoryboardGrid.tsx` to "Design a trip like this" for consistency.

